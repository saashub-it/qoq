{
  "name": "qoq",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build --verbose",
    "format": "npm run format:check -- --write",
    "format:check": "prettier --check \"./**/*\" --ignore-unknown"
  },
  "devDependencies": {
    "prettier": "3.0.3",
    "typescript": "5.2.2"
  }
}
